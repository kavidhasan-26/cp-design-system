export type IconProps = {
  /** Icon size in pixels. Defaults to the Figma artboard size. */
  size?: number;
  /** Primary icon color for monochrome icons. */
  color?: string;
  accessibilityLabel?: string;
};
